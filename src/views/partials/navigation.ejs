<nav class="navbar" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
        <a class="navbar-item" href="/">
            <img src="/logo.png" alt="ContractorPay logo" height="100px" />
        </a>

        <a
            role="button"
            class="navbar-burger"
            aria-label="menu"
            aria-expanded="false"
            data-target="navbarBasicExample"
        >
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
        </a>
    </div>

    <div id="navbarBasicExample" class="navbar-menu">
        <div class="navbar-start">
            <a href="/" class="navbar-item"> Home </a>
            <% if (locals.user) { %>
            <a href="/projects" class="navbar-item">Projects</a>
            <a href="/contractors" class="navbar-item">Contractors</a>
            <% } %>

            <div class="navbar-item has-dropdown is-hoverable">
                <a class="navbar-link"> More </a>

                <div class="navbar-dropdown">
                    <a href="/payment-categories" class="navbar-item">Payment Categories</a>

                    <!-- Admin link -->
                    <% if (locals.user && locals.user.role === 'admin') { %>
                    <a href="/admin" class="navbar-item">Admin</a>
                    <% } %>
                </div>
            </div>
        </div>

        <div class="navbar-end">
            <div class="navbar-item">
                <div class="buttons">
                    <% if (locals.user) { %>
                    <form action="/logout" method="post">
                        <button class="button is-seconary" type="submit">
                            <strong>Logout</strong>
                        </button>
                    </form>
                    <% } else { %>
                    <a class="button is-primary" href="/login">
                        <strong>Login</strong>
                    </a>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</nav>
